# Copy to .env and fill in values before running in production.

VAAS_SECRET_KEY=change-me-in-production
VAAS_DB_PATH=data/vaas.db
VAAS_HW_MODE=MOCK
VAAS_ARDUINO_PORT=/dev/ttyUSB0
VAAS_CAMERA_INDEX=0
