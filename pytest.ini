[pytest]
testpaths = tests
addopts = -v --tb=short --cov=src --cov=webapp
markers =
    integration: integration / system tests (excluded from unit count)
filterwarnings =
    ignore::DeprecationWarning
